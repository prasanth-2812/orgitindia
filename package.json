{
  "name": "orgit",
  "version": "1.0.0",
  "description": "ORGIT - Role-based mobile and web application for internal communication and task management",
  "private": true,
  "workspaces": [
    "mobile",
    "web",
    "backend",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix mobile && npm install --prefix web && npm install --prefix backend && npm install --prefix shared",
    "dev:backend": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix web",
    "dev:mobile": "npm run start --prefix mobile"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

